using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace ConsoleInput.Tests
{
    /// <summary>
    /// Tests the behavior of the press state.
    /// </summary>
    [TestClass]
    public class PressBehavior
    {
        /// <summary>
        /// We intend for the press state to turn itself off, after a single cycle of being enabled.
        /// </summary>
        [TestMethod]
        public void TestPressTurnsOff()
        {
            // Arrange

            // Act

            // Assert
        }

        /// <summary>
        /// We intend for the press state to turn itself on, after the last cycle of 'down' state.
        /// </summary>
        [TestMethod]
        public void TestPressTurnsOn()
        {
            // Arrange

            // Act

            // Assert
        }
    }
}
